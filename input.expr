programa
    INTEGER a, b, c,f,g,h.
	TEXTO d.
	REAL e.
	
	leia(a).
	leia(b).
	se(a > b) entao
		escreva(a).
		
	senao	
		escreva(b).
	fimse.
	
	leia(d).
	escreva(d).	

	c := 6.
	
	enquanto (c < 10) faca
		c := c + 1.
		escreva(c).
	fimenquanto.
	
	faca
		escreva("Teste Do-While").
	enquanto (c < 10).
	
	escreva("Insira um numero real: ").
	leia(e).
	escreva(e).
	
	f := 10+5*2.
	
fimprog.