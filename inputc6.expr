programa
    INTEGER a.

    a := 2.
	a := a + 8.
    faca
        escreva("Incremente a 3 vezes imprimindo o valor de e todas as vezes").
        leia(a).
        escreva(a).
    enquanto(a < 5).
    escreva("Fim").

fimprog.